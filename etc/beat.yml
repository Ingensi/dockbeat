################### Dockerbeat Configuration Example #########################

############################# Dockerbeat ######################################

dockerbeat:
  # Defines how often an event is sent to the output
  period: 1s
